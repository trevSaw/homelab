# Naming Convention Standard

Directories use lowercase kebab-case. Markdown files use PascalCase with a `.md` extension, with `README.md` as the standard index exception. ADRs use `ADR-0001-ShortTitle.md`.
