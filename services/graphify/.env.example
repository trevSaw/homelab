# Graphify environment template — Phase 14.4
# Copy to .env and adjust. Do not commit .env.

TZ=Asia/Tokyo

# Optional API key for the MCP HTTP server (Authorization: Bearer / X-API-Key).
# If empty, the server is unauthenticated on the internal Docker network.
GRAPHIFY_API_KEY=replace-with-a-high-entropy-key
