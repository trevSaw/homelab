services:
  logseq:
    image: ghcr.io/logseq/logseq-webapp:latest
    container_name: logseq-web
    restart: unless-stopped
    ports:
      - "3001:80"
    volumes:
      - /hive/echoos:/graph
    environment:
      LOGSEQ_GRAPH: /graph
    networks:
      - echoos_net
