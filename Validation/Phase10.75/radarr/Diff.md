# Diff vs prior compose source

- CasaOS image was linuxserver/radarr:latest; live is ghcr.io/hotio/radarr:latest — import uses live
- CasaOS downloads bind was /hive/downloads/completed/Radarr; live is /hive/downloads — import uses live
- Stripped x-casaos metadata and icon label
